# Standalone 50/50 DMS/MPS oil example. Run: ./Generator/oil_generator --config model.conf
length = 32
chains = 500
mps_percent = 50
sequence = random
density = 0.1
target_density = 0.8
# The film job later reads the bulk 300 K NPT snapshot and adds one 300 K
# repulsive-wall cutoff of z padding on each face by default.
min_separation = 4.5
seed = 20260727
velocity_seed = 492845
